from enum import Enum


class MongoOperators(Enum):
    EQUAL = "$eq"
    NOT_EQUAL = "$ne"
    GREATER_THAN = "$gt"
    GREATER_THAN_EQUAL = "$gte"
    LESS_THAN = "$lt"
    LESS_THAN_EQUAL = "$lte"
    IN = "$in"
    NOT_IN = "$nin"
    OR = "$or"
    AND = "$and"
    NOT = "$not"
    NOR = "$nor"
    EXISTS = "$exists"
    TYPE = "$type"
    MOD = "$mod"
    REGEX = "$regex"
    TEXT = "$text"
    WHERE = "$where"
    ALL = "$all"
    ELEMENT_MATCH = "$elemMatch"
    SIZE = "$size"
    SLICE = "$slice"
    PUSH = "$push"
    PULL = "$pull"
    PULL_ALL = "$pullAll"
    ADD_TO_SET = "$addToSet"
    POP = "$pop"
    INC = "$inc"
    MUL = "$mul"
    RENAME = "$rename"
    SET_ON_INSERT = "$setOnInsert"
    SET = "$set"
    UNSET = "$unset"
    MIN = "$min"
    MAX = "$max"
    CURRENT_DATE = "$currentDate"
    TIMESTAMP = "$timestamp"
    BIT = "$bit"
    ISOLATED = "$isolated"
    COMMENT = "$comment"
    HINT = "$hint"
    MAX_TIME_MS = "$maxTimeMS"
    ORDER_BY = "$orderby"
    ORDER_BY_DESC = "$orderbydesc"
    TEXT_SEARCH = "$textsearch"
    TEXT_SCORE = "$textscore"
    TEXT_LANGUAGE = "$textlanguage"
    TEXT_CASE_SENSITIVE = "$textcasesensitive"
    TEXT_DIACRITICS_SENSITIVE = "$textdiacriticssensitive"
    TEXT_PUNCTUATION_SENSITIVE = "$textpunctuationsensitive"
    TEXT_MIN = "$textmin"
    TEXT_MAX = "$textmax"
    TEXT_WEIGHT = "$textweight"
    TEXT_INDEX_VERSION = "$textindexversion"
    TEXT_DEFAULT_LANGUAGE = "$textdefaultlanguage"
    TEXT_LANGUAGE_OVERRIDE = "$textlanguageoverride"
    TEXT_SEARCH_MODE = "$textsearchmode"
    TEXT_DICTIONARY_OVERRIDE = "$textdictionaryoverride"
    TEXT_OPERATOR = "$textoperator"
    TEXT_QUERY = "$textquery"
    TEXT_PATH = "$textpath"
    TEXT_META = "$textmeta"
